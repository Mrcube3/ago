export const processViaAutoVeo = async (data: string) => {
  console.log("Submitting to Veo API:", data);
  // Future: await circleW3S.executeTransaction(...)
};

export const processViaNanoBanana = async (data: string) => {
  console.log("Submitting to Banana RPC:", data);
  // Future: await arcProvider.sendTransaction(...)
};
